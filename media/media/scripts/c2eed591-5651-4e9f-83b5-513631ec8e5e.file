#include <bits/stdc++.h>

using namespace std;
int main() {
    string s;
    cin >> s;
    string l = "TOSHKENT";
    string ans = "";
    for (char i : s) {
        if (find(l.begin(), l.end(), i) == l.end()) {
            ans += i;
        }
    }
    cout << ans << '\n';
}