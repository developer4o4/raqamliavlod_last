#include <bits/stdc++.h>
using namespace std;
#define int long long
#define len(a) (int)a.size()
void solve(){ 
  int n,m;cin >> n >> m;
  int s = 0;
  vector<int> a(m);for(int &x:a) cin >> x,s += x;
  cout << (s+n-1)/n;
}
signed main(){
  #ifdef DEBUG_
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
  #endif
  int q = 1;//cin >> q;
  while(q--) solve();
  return 0;
}