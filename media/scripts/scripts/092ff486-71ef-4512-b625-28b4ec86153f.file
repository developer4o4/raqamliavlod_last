#include <iostream>
#include <string>
#include <set>

using namespace std;

string DtsL(string input) {
    set<char> letters = {'T', 'O', 'S', 'H', 'K', 'E', 'N'};
    string result = "";
    for (char c : input) {
        if (letters.find(c) == letters.end()) {
            result += c;
        }
    }
    return result;
}

int main() {
    string input;
    getline(cin, input);
    cout << DtsL(input) << endl;

    return 0;
}
