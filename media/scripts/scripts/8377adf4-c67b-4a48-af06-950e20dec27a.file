#include <bits/stdc++.h>

using namespace std;

int main() {
	int n, m;
	cin >> n, m = n / 2;
	vector<int> a(n);
	for (int &i : a) cin >> i;
	for (int i : a) if (i == 1) n--;
	n = max(n, m);
	cout << n;
	return 0;
}