#include <algorithm>
#include <bits/stdc++.h>

using namespace std;

using ll = long long;

ll f(ll n) {
	return (n >= 6) + max(0ll, n - 6) / 12;
}

int main() {
	ll a, b, c;
	cin >> a >> b;
	c = (b - a + 1) * 2 + b / 2 - (a - 1) / 2 + f(b) - f(a - 1);
	cout << c;
	return 0;
}
