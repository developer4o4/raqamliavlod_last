#include <bits/stdc++.h>
using namespace std;
#define int long long 
signed main(){
    string a;cin>>a;
    for (int i = 0; i < a.length(); i++) {
        if(a[i]!='T' && a[i]!='O' && a[i]!='S' && a[i]!='H' && a[i]!='K' && a[i]!='E' && a[i]!='N'){
            cout<<a[i];
        }
    }
}