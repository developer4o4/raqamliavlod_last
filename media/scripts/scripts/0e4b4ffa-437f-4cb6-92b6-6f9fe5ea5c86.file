#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	string a, b = "";
	cin >> a;
	for(int i = 0; i < (int) a.length(); i++){
		if(a[i] != 'T' && a[i] != 'O' && a[i] != 'S' && a[i] != 'H' && a[i] != 'K' && a[i] != 'E' && a[i] != 'N' && a[i] != 'T') b += a[i];
	}
	cout << b << endl;
	return 0;
}
