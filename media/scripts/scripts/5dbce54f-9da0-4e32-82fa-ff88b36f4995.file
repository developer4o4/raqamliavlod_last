def smallest(n):
    i = 2
    while n % i == 0:
        i += 1
    return i

def strenght(n):
    count = 1
    while n != 2:
        n = smallest(n)
        count += 1
    return count

def total_strength(A, B):
    total = 0
    for n in range(A, B + 1):
        total += strenght(n)
    return total

A, B = map(int, input().split())

print(total_strength(A, B))
