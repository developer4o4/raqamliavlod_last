#include<bits/stdc++.h>
using namespace std;
int main(){
    string a;
    cin >> a;
    map<char, int> mp;
    string tosh = "TOSHKENT";
    for(int i = 0; i < tosh.size(); i ++){
        mp[tosh[i]] = 1;
    }
    for(int i = 0; i < a.size(); i ++){
        if(!mp[a[i]]) cout << a[i];
    }
}