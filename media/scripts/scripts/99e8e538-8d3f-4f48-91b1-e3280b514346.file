#include <bits/stdc++.h>
#define int long long	
using namespace std;
void solve(){
	string s,m="TOSHKENT";
	cin>>s;
	sort(m.begin(),m.end());
	for(char c:s){
		if(binary_search(m.begin(),m.end(),c)) continue;
		else cout<<c;
	}
}
signed main() {
    ios_base::sync_with_stdio(0);
    cout.tie(0);
    cin.tie(0);
    int t=1;
    //int t; cin>>t;
    while(t--){
        solve();
    }
    return 0;
}
