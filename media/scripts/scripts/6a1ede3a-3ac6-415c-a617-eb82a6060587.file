#include<bits/stdc++.h>
using namespace std;
#define int long long
signed main(){
    string a="TOSHKENT";
    string s;cin>>s;
    for(int i=0;i<s.size();i++){
        bool ok = false;
        for(int j=0;j<a.size();j++){
            if(s[i]==a[j])ok=true;
        }
        if(!ok)cout<<s[i];
    }
}