#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);  // Zanjirlar sonini o'qish
    
    int chains[N];
    
    // Zanjirlar uzunligini o'qish
    for (int i = 0; i < N; i++) {
        scanf("%d", &chains[i]);
    }

    // Natija: Har doim faqat 1 ta halqa ochish va yopish kerak.
    // Shunday qilib, biz faqat bitta halqani ochish va yopishimiz kerak
    // barcha zanjirlarni ulash uchun.
    printf("1\n");

    return 0;
}
