#include <iostream>
#include <string>
using namespace std;

int main() {
 
    string word;
    cin >> word;

    string remove_letters = "TOSHKENT";
    string result = "";

    for (char ch : word) {

        if (remove_letters.find(ch) == string::npos) {
            result += ch;
        }
    }
    cout << result << endl;
    return 0;
}
