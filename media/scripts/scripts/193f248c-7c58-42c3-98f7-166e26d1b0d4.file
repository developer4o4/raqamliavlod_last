#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;

    if (n < 38 || n < (n / 5 + 1) * 5 - 2) {
        cout << n << endl;
    } else {
        cout << (n / 5 + 1) * 5 << endl;
    }

    return 0;
}