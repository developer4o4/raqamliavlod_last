def possible_champions(N, points):
    max_points = max(points)
    champions = 0
    for score in points:
        if score + N >= max_points:
            champions += 1
    return champions
N = int(input())
points = [int(input()) for i in range(N)]
print(possible_champions(N, points))