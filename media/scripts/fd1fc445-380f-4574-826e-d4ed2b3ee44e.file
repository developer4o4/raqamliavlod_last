#include <bits/stdc++.h>

using namespace std;

#define int long long

int lcm(int a, int b) {
    return a / __gcd(a, b) * b;
}

signed main() {
    int a, b;
    cin >> a >> b;
    
    int ans = b - a + 1;
    int k = 1;
    
    for (int i = 1; i <= 41; i ++) {
        k = lcm(k, i);
        int x = b / k - (a - 1) / k;
        //cout << x << endl;
        
        ans += x;
    }
    
    cout << ans << endl;
    
    return 0;
}