#include <bits/stdc++.h>

using namespace std;

#define int long long

signed main() {
	int a, b;
	cin >> a >> b;
	vector<int> c(15);
	c[0]++;
	for (int i = 0; i < b; ++i) {
		string s;
		for (int j = 0; j < 15; ++j) {
			if (!c[j]) break;
			s.push_back(char(c[j] + 'a' - 1));
		}
		c[0]++;
		for (int j = 1; j < 15; ++j) {
			if (!c[j - 1]) break;
			c[j] += c[j - 1] / 27;
			c[j - 1] %= 27;
			if (!c[j - 1]) c[j - 1]++;
		}
		cout << s << ' ';
	}
	return 0;
}