#include "bits/stdc++.h"
using namespace std;
int main(){
    int n; cin >> n;
    int a[n];
    for(int i = 0; i < n; ++ i) cin >> a[i];
    sort(a, a + n, greater<int>());
    int mx = 0, ans = 0;
    for(int i = 0; i < n; ++ i) ans += a[i] + n >= mx, mx = max(mx, a[i]+i+1);
    cout << ans;
}
/*


a[i]

[i + 1, n]
cur = 1
for i n...i+1: a[i]+=cur
max([i+1,n])<=a[i]
*/
