def strength(N):
    result = 0
    while N > 1:
        result += 1
        N = (N + 1) // 2
    return result

def total_strength(A, B):
    total = 0
    for i in range(A, B+1):
        total += strength(i)
    return total

# Kirish
A, B = map(int, input().split())

# Chiqish
print(total_strength(A, B))